<nav aria-label="Main">
  <ul class="nav-container">
    <li><a href="/" class="nav-item">Home</a></li>
    <li><a href="/about" class="nav-item">About</a></li>
    <li><a href="/artists" class="nav-item">Artists</a></li>
    <li><a href="/aftercare" class="nav-item">Aftercare</a></li>
    <li><a href="#" class="nav-item">Booking</a></li>
    <li><a href="#" class="nav-item">Contact</a></li>
  </ul>
</nav>

<style>
  .nav-container {
    display: flex;
    gap: 1rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-item {
    color: #faf8f5;
    text-decoration: none;
    font-weight: bold;
  }

  nav {
    &[aria-label='Main'] {
      box-sizing: border-box;
      position: fixed;
      /* width: 100vw; */
      /* height: 100vh; */
      background-color: black;
      background-repeat: no-repeat;
      background-position: 80% 80%;
      top: 0;
      left: 0;
      padding: 2rem;
      /* transition: all 0.3s ease-in-out; */
      /* translate: -100vw 0; */
      z-index: 100;

      /* &[data-mobile-nav-open='true'] {
        translate: 0 0;
      } */
    }

    & ul {
      padding: 0;
      list-style: none;

      & li {
        line-height: 1;
        margin-block-end: 2rem;

        & a {
          color: var(--white);
          font-weight: 700;
          font-family: var(--heading-font);
          font-size: 2rem;
          text-transform: uppercase;
          transition: all 200ms;

          &:hover {
            color: var(--highlight-color);
            scale: 1.1;
          }

          &:active {
            color: var(--white);
            scale: 1;
          }

          &[aria-current='page'] {
            color: var(--highlight-color);
          }
        }
      }
    }

    &[aria-label='Secondary'] {
      & ul {
        display: flex;

        & li {
          align-items: center;
          display: flex;
          line-height: 1;
          margin-block-end: 0;
          margin-inline-end: 1rem;

          &:last-of-type {
            margin-inline-end: 0;
          }
        }
      }
    }
  }
</style>
